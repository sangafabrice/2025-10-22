{
  "name": "fttl-yt-thumb",
  "version": "1.0.0",
  "main": "src/fttl-yt-thumb.js",
  "type": "module",
  "scripts": {
    "build": "build",
    "build:js": "rollup --config build/rollup.config.js",
    "build:css": "cd src/templates/assets&& postcss *.css --dir bin --ext min.css --use postcss-preset-env --use cssnano --no-map",
    "build:html": "cd src/templates/assets&& (dir bin > nul 2>&1 || mkdir bin)&& html-minifier-terser template.html --output bin/template.min.html --collapse-whitespace",
    "build:svg": "cd src/templates/assets&& (dir bin > nul 2>&1 || mkdir bin)&& svgo --input yt-play-icon.svg --output bin/yt-play-icon.min.svg --multipass",
    "watch:10": "watch --root=src --ignore=bin/ --delay=10000 build",
    "watch": "watch --root=src --ignore=bin/ build"
  },
  "keywords": [],
  "author": "Fabrice Sanga",
  "license": "ISC",
  "description": "YouTube thumbnaiil web component",
  "workspaces": [ "build/", "watch/" ]
}
